# تطوير محلي (بدون Docker)
DATABASE_URL="file:./dev.db"

# إنتاج / Docker — فعّل بعد: docker compose up -d
# DATABASE_URL="postgresql://decozr:password123@localhost:5432/decozr_db"
JWT_SECRET="change-me-in-production"
PORT=3000
CORS_ORIGIN="http://localhost:5173"

# إشعارات واتساب للعملاء عند تغيير حالة الطلب (اختياري)
# أرسل POST JSON إلى جسر (n8n / Meta Cloud / Twilio / CallMeBot)
# WHATSAPP_WEBHOOK_URL=https://your-bridge.example/whatsapp
# WHATSAPP_WEBHOOK_TOKEN=secret
